import { DatePeriodPipe } from './date-period-pipe';

describe('DatePeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
