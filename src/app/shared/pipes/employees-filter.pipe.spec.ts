import { EmployeesFilterPipePipe } from './employees-filter.pipe';

describe('EmployeesFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeesFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
