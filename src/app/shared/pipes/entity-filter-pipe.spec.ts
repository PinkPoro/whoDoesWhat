import { EntityFilterPipe } from './entity-filter-pipe';

describe('EntityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
